#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string followed by a new line to stdout
 *
 * @str: parameter that takes the string
 *
 * Return: Always 0 or 1
 */
void _puts(char *str)
{
	puts(str);
}
