#include "main.h"
#include <string.h>
/**
 * _strlen - To check the size of string
 *
 * @s: parameter for testing
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
