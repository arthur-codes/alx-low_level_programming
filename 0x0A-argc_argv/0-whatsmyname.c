#include "main.h"
#include <stdio.h>
/**
 * main - Entry point of program
 *
 * @argc: argument count parameter
 *
 * @argv: argument count array parameter
 *
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
