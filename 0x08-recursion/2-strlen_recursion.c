#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - prints the length of a string
 *
 * @s: parameter for testing
 *
 * Return: Always 1
 */
int _strlen_recursion(char *s)
{
	int n = strlen(s);

	return (n);
}
