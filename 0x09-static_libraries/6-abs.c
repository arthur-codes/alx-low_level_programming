#include "main.h"
#include <stdlib.h>
/**
 * _abs - print the absolute value of a number
 *
 * @c: an integer parameter for testing
 *
 * Return: Always 0 (Success)
 */
int _abs(int c)
{
	return (abs(c));
}
